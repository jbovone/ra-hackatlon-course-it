import styled from "@emotion/styled";

const Footer = styled.footer({
  background: "black",
  height: "4vh",
  width: "100%",
  position: "fixed",
  bottom: 0,
  padding: 5,
});

export default Footer;
